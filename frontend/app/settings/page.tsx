import { SectionPlaceholder } from "../../components/layout/SectionPlaceholder";

export default function SettingsPage() {
  return <SectionPlaceholder eyebrow="Configuration" title="Settings" description="Prepare configurações da aplicação e da identidade white-label." />;
}
